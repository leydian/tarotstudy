# Legacy Pages

이 디렉터리는 현재 라우트에서 사용하지 않는 이전 실험/학습용 페이지를 보관합니다.

- `ChatReading.tsx`
- `Reading.tsx`
- `StudyReading.tsx`

운영 화면에는 노출되지 않으며, 재도입할 때는 현재 `TarotMastery`/`OpsDashboard` 구조와 계약을 기준으로 재검토하세요.
