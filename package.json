{
  "name": "studycodex2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @tarot/api",
    "dev:web": "npm run dev -w @tarot/web",
    "install:all": "npm install",
    "build:web": "npm run build -w @tarot/web",
    "a11y:contrast": "node scripts/wcag-contrast-check.mjs",
    "vercel-build": "npm run build:web",
    "build": "npm run build:web",
    "release:note": "node scripts/generate-release-note.js"
  }
}
